{% extends 'base.html.twig' %}

{% block title %}PurchaseOrder{% endblock %}

{% block body %}
    <h1>PurchaseOrder</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ purchase_order.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ purchase_order.email }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ purchase_order.phone }}</td>
            </tr>
            <tr>
                <th>Delivery</th>
                <td>{{ purchase_order.delivery }}</td>
            </tr>
            <tr>
                <th>Payment</th>
                <td>{{ purchase_order.payment }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('purchase_order_index') }}">back to list</a>

    <a href="{{ path('purchase_order_edit', {'id': purchase_order.id}) }}">edit</a>

    {{ include('purchase_order/_delete_form.html.twig') }}
{% endblock %}
